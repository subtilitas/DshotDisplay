# Doxygen output
docs/html/
docs/latex/
# Local configuration files
localdata
# Build trees (CMake, PlatformIO) and firmware artefacts
build/
.pio/
*.uf2
*.elf
*.bin
*.hex

# Host test build output
test/dshot_tests
test/gen_blackbox_log
test/*.ppm
test/*.BFL

# Scratch space for files being retired
_to_delete/

# Wiki screenshots, rendered from the host suite by
#   python3 tools/make_previews.py --wiki
# Not committed: they are a function of the UI code, CI regenerates them on
# every run, and 24 PNGs of churn per layout tweak buys nothing.
wiki/img/
